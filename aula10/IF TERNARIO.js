let media = 7;
/*let resultado = '';
if (media > 7) {
    resultado = 'Aprovado';
} else {
    resultado = 'Reprovado'
}
*/

//let resultado = media > 7 ? 'Aprovado' : 'Reprovado';

// ANINHAMENTO
let resultado = media >= 7 ? 'Aprovado' :
media > 5 && media < 7 ? 'Recuperação' : 'Reprovado';